# NoSkid DB Init script
This folder contains the initialisation script for your NoSkid API DataBase.
Run the `setup.sql` file into a fresh database, and it will build it with the latest version of the NoSkid API Database.